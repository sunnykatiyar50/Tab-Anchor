{
  "name": "tab-anchor",
  "version": "1.0.4",
  "description": "Browser extension to anchor, rename, and protect tabs",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js"
  },
  "devDependencies": {
    "adm-zip": "^0.5.16"
  }
}
